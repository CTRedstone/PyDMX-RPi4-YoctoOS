name: Fix pydmx files

on:
  workflow_dispatch:

jobs:
  unpack:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Unzip pydmx files
        run: |
          cd /home/runner/work/PyDMX-RPi4-YoctoOS/PyDMX-RPi4-YoctoOS/meta-pydmx/recipes-pydmx/pydmx/files
          unzip Archiv.zip -d .

      - name: Commit changes
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add /home/runner/work/PyDMX-RPi4-YoctoOS/PyDMX-RPi4-YoctoOS/meta-pydmx/recipes-pydmx/pydmx/files
          git commit -m "Add pydmx files via GH Actions" || echo "No changes"
          git push
